trigger:
- none

pool: agent1_pool

variables:
  TOMCAT_HOME: /opt/tomcat
  WAR_NAME: JavaLoginShowcase.war

stages:
# =========================
# CI STAGE – BUILD
# =========================
- stage: Build
  displayName: Build WAR
  jobs:
  - job: MavenBuild


    steps:
    - checkout: self

    - script: |
        java -version
        mvn -version
        cd JavaLoginShowcase
        mvn clean package -DskipTests
        displayName: Build WAR with Maven


    - task: PublishBuildArtifacts@1
      displayName: Publish WAR Artifact
      inputs:
        PathtoPublish: JavaLoginShowcase/target
        ArtifactName: drop
        